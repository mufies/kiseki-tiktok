# Development Dockerfile for .NET Services with dotnet watch (Hot Reload)
FROM mcr.microsoft.com/dotnet/sdk:8.0

# Set working directory
WORKDIR /app/NotificationService

# Copy proto files (needed by csproj)
COPY proto/ ../../proto/

# Copy csproj and restore dependencies
COPY notification-service/NotificationService/*.csproj ./
RUN dotnet restore

# Copy everything else
COPY notification-service/NotificationService/ ./

# Install dotnet tools
RUN dotnet tool install --global dotnet-ef
ENV PATH="${PATH}:/root/.dotnet/tools"

# Expose ports
EXPOSE 8085 9093

# Enable hot reload with dotnet watch
# Proto files will be auto-generated by Grpc.Tools during build
CMD ["dotnet", "watch", "run", "--non-interactive", "--no-restore", "--urls", "http://0.0.0.0:8085"]
